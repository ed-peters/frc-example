# Intake Commands

These are commands that work with the `IntakeSubsystem`. They're simple, but a few learnings are embedded here ...

**Tuning is easy**. You just run the RPS command with different kV and kP values. Start by getting kV really accurate, then kP can be pretty small. You probably won't need kD at all.
